'use strict';

var app = require('../server');
var chai = require('chai');
var request = require('supertest');

var expect = chai.expect;

describe('Mongo database test', function() {
  describe('post to database', function() {
    it('should post url', function(done) {

    });

    it('should post id', function(done) {

    });
  });

  describe('get from database', function() {
    it('should get all the ids', function(done) {

    });
  });

});
